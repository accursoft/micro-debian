#!/bin/sh -eu

docker pull debian:jessie
cd perl
./build
cd ../no-perl
./build
cd ..
docker run --rm -e DEBIAN_FRONTEND=noninteractive accursoft/micro-jessie:no-perl sh -c "apt-get update && apt-get install -y perl-base curl && curl https://google.com >/dev/null"